const price = 10;                                                      // price of one item
var number_of_items = 20;                                              // number of items in my shop

var total_price = number_of_items*price;                               // formula to calculate total price 
// price = price + 1 ;(constant ki value change nhi ho skti)           // price of one item changed

console.log(total_price);                                              // printed the total price
